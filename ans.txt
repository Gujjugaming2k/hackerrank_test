
if not df_filtered.empty and df_filtered['duration'].notna().any():
    movie_name = df_filtered.loc[df_filtered['duration'].idxmax(), 'title']
else:
    movie_name = "No valid movie found"

print("Movie with highest duration:", movie_name)
